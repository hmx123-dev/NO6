# 就诊通知功能修改说明

## 一、功能概述

本次修改实现了以下功能：

1. **预约成功后立即发送所有后续提醒**：用户预约审核通过后，系统会立即创建并发送所有后续提醒通知（预约成功通知、就诊前一天提醒、就诊当天提醒、就诊后随访提醒）。

2. **用户接收状态判断**：系统会记录用户的通知接收状态（未接收、已接收、已读）。

3. **通知发送失败的系统记录和重试机制**：
   - 记录每次通知发送的状态
   - 发送失败时记录失败原因
   - 支持自动重试（最多3次）
   - 支持管理员手动重试

4. **管理员后台查看与处理**：
   - 查看所有通知记录
   - 按状态筛选通知
   - 查看通知详情
   - 手动重试发送失败的通知
   - 批量重试
   - 查看通知统计

## 二、数据库变更

### 1. 新建表：通知记录表（tongzhijilu）

```sql
CREATE TABLE IF NOT EXISTS `tongzhijilu` (
  `id` bigint(20) NOT NULL AUTO_INCREMENT COMMENT '主键',
  `addtime` timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `yuyuebianhao` varchar(200) COLLATE utf8mb4_unicode_ci DEFAULT NULL COMMENT '预约编号',
  `yishengzhanghao` varchar(200) COLLATE utf8mb4_unicode_ci DEFAULT NULL COMMENT '医生账号',
  `zhanghao` varchar(200) COLLATE utf8mb4_unicode_ci DEFAULT NULL COMMENT '用户账号',
  `tongzhileixing` int(11) DEFAULT NULL COMMENT '通知类型（1-预约成功通知 2-就诊前一天提醒 3-就诊当天提醒 4-就诊后随访提醒）',
  `tongzhineirong` varchar(500) COLLATE utf8mb4_unicode_ci DEFAULT NULL COMMENT '通知内容',
  `fasongshijian` datetime DEFAULT NULL COMMENT '发送时间',
  `fasongzhuangtai` int(11) DEFAULT '0' COMMENT '发送状态（0-待发送 1-发送成功 2-发送失败）',
  `chongshicishu` int(11) DEFAULT '0' COMMENT '重试次数',
  `jieshouzhuangtai` int(11) DEFAULT '0' COMMENT '用户接收状态（0-未接收 1-已接收 2-已读）',
  `jieshoushijian` datetime DEFAULT NULL COMMENT '接收时间',
  `shibaiyuanyin` varchar(500) COLLATE utf8mb4_unicode_ci DEFAULT NULL COMMENT '失败原因',
  `jihuafasongshijian` datetime DEFAULT NULL COMMENT '计划发送时间',
  `jiuzhenshijian` datetime DEFAULT NULL COMMENT '就诊时间',
  PRIMARY KEY (`id`),
  KEY `yuyuebianhao` (`yuyuebianhao`),
  KEY `fasongzhuangtai` (`fasongzhuangtai`),
  KEY `jihuafasongshijian` (`jihuafasongshijian`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='通知记录';
```

执行SQL文件：`server_code/sql/tongzhijilu_table.sql`

## 三、后端代码变更

### 1. 新增文件

| 文件路径 | 说明 |
|---------|------|
| `com.cl.entity.TongzhijiluEntity` | 通知记录实体类 |
| `com.cl.entity.view.TongzhijiluView` | 通知记录视图类 |
| `com.cl.dao.TongzhijiluDao` | 通知记录DAO接口 |
| `com.cl.service.TongzhijiluService` | 通知记录服务接口 |
| `com.cl.service.impl.TongzhijiluServiceImpl` | 通知记录服务实现 |
| `com.cl.controller.TongzhijiluController` | 通知记录控制器 |
| `com.cl.task.NotificationTask` | 通知定时任务 |
| `resources/mapper/TongzhijiluDao.xml` | MyBatis映射文件 |

### 2. 修改文件

| 文件路径 | 修改内容 |
|---------|---------|
| `com.cl.controller.YishengyuyueController` | 在审核通过时调用创建通知记录方法 |
| `com.cl.SpringbootSchemaApplication` | 添加`@EnableScheduling`注解启用定时任务 |

## 四、前端代码变更

### 1. 新增文件

| 文件路径 | 说明 |
|---------|------|
| `manage_code/src/views/tongzhijilu/list.vue` | 通知记录管理列表页面 |

### 2. 修改文件

| 文件路径 | 修改内容 |
|---------|---------|
| `manage_code/src/router/index.js` | 添加通知记录路由 |

## 五、菜单配置

需要在后台管理系统的菜单管理中添加"通知记录"菜单：

1. 登录管理员后台
2. 进入"菜单管理"页面
3. 在"系统管理"菜单下添加子菜单：
   - 菜单名称：通知记录
   - 路由路径：tongzhijilu
   - 权限按钮：查看、重试

## 六、定时任务说明

系统启动后会自动启动以下定时任务：

1. **每分钟执行一次**：检查待发送且计划发送时间已到的通知，进行发送
2. **每5分钟执行一次**：重试发送失败且重试次数小于3次的通知

## 七、API接口说明

### 通知记录相关接口

| 接口地址 | 请求方式 | 说明 |
|---------|---------|------|
| `/tongzhijilu/page` | GET | 分页查询通知记录 |
| `/tongzhijilu/info/{id}` | GET | 获取通知详情 |
| `/tongzhijilu/retry/{id}` | POST | 重试发送通知 |
| `/tongzhijilu/retryBatch` | POST | 批量重试发送 |
| `/tongzhijilu/updateReceiveStatus` | POST | 更新接收状态 |
| `/tongzhijilu/statistics` | GET | 获取通知统计 |

## 八、通知类型说明

| 类型值 | 类型名称 | 发送时机 |
|-------|---------|---------|
| 1 | 预约成功通知 | 预约审核通过后立即发送 |
| 2 | 就诊前一天提醒 | 就诊前一天上午9:00 |
| 3 | 就诊当天提醒 | 就诊当天早上8:00 |
| 4 | 就诊后随访提醒 | 就诊后第二天上午10:00 |

## 九、发送状态说明

| 状态值 | 状态名称 | 说明 |
|-------|---------|------|
| 0 | 待发送 | 已创建但未到发送时间 |
| 1 | 发送成功 | 通知已成功发送 |
| 2 | 发送失败 | 通知发送失败，可重试 |

## 十、接收状态说明

| 状态值 | 状态名称 | 说明 |
|-------|---------|------|
| 0 | 未接收 | 用户尚未接收到通知 |
| 1 | 已接收 | 用户已接收到通知 |
| 2 | 已读 | 用户已阅读通知 |

## 十一、使用说明

### 1. 正常流程

1. 用户提交预约申请
2. 医生/管理员审核预约（选择"通过"）
3. 系统自动创建4条通知记录：
   - 预约成功通知（立即发送）
   - 就诊前一天提醒（定时发送）
   - 就诊当天提醒（定时发送）
   - 就诊后随访提醒（定时发送）
4. 定时任务按计划发送待发送的通知

### 2. 处理发送失败的通知

1. 管理员进入"通知记录"页面
2. 筛选发送状态为"发送失败"的记录
3. 点击"重试"按钮手动重试，或选择多条记录后点击"批量重试"
4. 查看失败原因，根据具体情况处理

### 3. 查看通知统计

1. 点击"统计"按钮
2. 查看待发送、发送成功、发送失败、总计数量

## 十二、注意事项

1. 通知发送功能目前为模拟实现，实际项目中需要接入短信服务商（如阿里云短信、腾讯云短信）或推送服务（如极光推送）。

2. 修改`TongzhijiluServiceImpl.doSend()`方法，接入实际的通知发送服务。

3. 用户接收状态的更新需要前端配合，当用户查看通知时调用`/tongzhijilu/updateReceiveStatus`接口。

4. 定时任务使用Spring的`@Scheduled`注解实现，如需更复杂的调度，可以考虑集成Quartz。

5. 通知记录表数据量可能较大，建议定期清理历史数据或进行归档。
